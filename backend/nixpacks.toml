[phases.setup]
nixPkgs = ["python3", "gcc", "python3.10-venv"]

[phases.install]
cmds = [
  "python -m venv --copies /opt/venv",
  ". /opt/venv/bin/activate",
  "pip install -r requirements.txt",
  "python -m spacy download en_core_web_sm"
]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT"
