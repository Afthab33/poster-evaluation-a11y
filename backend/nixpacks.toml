[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "python311Packages.virtualenv", "tesseract", "tesseract4", "cmake"]

[phases.install]
cmds = ["python -m virtualenv /opt/venv", "source /opt/venv/bin/activate", "pip install -r requirements.txt"]

[start]
cmd = "source /opt/venv/bin/activate && gunicorn app:app"